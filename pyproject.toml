[project]
name = "gptpoc"
version = "0.1.0"
description = "Continue-thinking middleware for the Codex/GPT API."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "starlette>=0.37",
    "uvicorn>=0.30",
    "websockets>=13",
]

[dependency-groups]
dev = []
